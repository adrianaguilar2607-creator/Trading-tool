import React from "react"
import ReactDOM from "react-dom/client"
import TradingTool from "./TradingTool.jsx"

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TradingTool />
  </React.StrictMode>,
)
